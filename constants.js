const STATUS = {
  normal: 'normal',
  disabled: 'disabled',
  enabled: 'enabled'
}

const EMAIL_STATUS = {
  waiting: 'waiting',
  sending: 'sending',
  sent: 'sent',
  error: 'error',
}


module.exports = {
  STATUS,
  EMAIL_STATUS
}
